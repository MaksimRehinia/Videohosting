@model Videohosting.Models.ApplicationUser
@if (Model != null)
{
    <ul>
        @foreach (var subscribe in Model.Chanels)
        {
            if (subscribe.User.UserName != Model.UserName)
            {
                <li>@Html.ActionLink(subscribe.User.UserName, "Index", "Chanel", new { subscribe = subscribe.User.UserName });</li>
            }
        }
    </ul>
}
