@model Videohosting.Models.Video
@{
    ViewBag.Title = "Home Page";
}

@if (User.IsInRole("User"))
{
    
}
<video style="outline:none; width: 60%;" > 
<source src="@Url.Action("Index","Video", new {filePath="1.mp4"})" type="video/mp4"> 
</video>